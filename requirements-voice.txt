# Voice generation (Coqui XTTS v2). Install PyTorch first, then:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements-voice.txt
#
# silero-stress — MIT (Silero Team). Used for Russian ё/stress prep before XTTS.
#
# transformers MUST stay on 4.x: coqui-tts 0.27.x imports
# transformers.pytorch_utils.isin_mps_friendly (removed/broken on transformers 5.x).
# Known advisories on 4.57.x (Trainer / malicious Hub configs) do not apply to this
# mod's path (local XTTS inference only, no Trainer, no untrusted from_pretrained).
coqui-tts>=0.27.5
transformers>=4.57,<5
silero-stress>=1.4
