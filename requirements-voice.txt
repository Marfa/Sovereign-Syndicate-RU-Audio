# Voice generation (Coqui XTTS v2). Install PyTorch first, then:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements-voice.txt
#
# silero-stress — MIT (Silero Team). Used for Russian stress before XTTS.
# StressRNN is NOT a dependency of this project (Apache-2.0 alternative, unused).
coqui-tts>=0.27.5
transformers>=4.57,<5
silero-stress>=1.4
